# Use unicode
export LC_CTYPE=en_US.UTF-8

#export CLICOLOR='YES'
#export LSCOLORS='ExGxFxdxCxDxDxhbadExEx'
#export LSCOLORS='di=1:fi=0:ln=31:pi=5:so=5:bd=5:cd=5:or=31:mi=0:ex=35:*.rpm=90'

# Editor
export EDITOR='vim'
export GIT_EDITOR='vim'
export SVN_EDITOR='vim'

# Path config
export PATH=/usr/local/bin:/usr/local/sbin:/usr/sbin:$PATH
export PATH=~/pear:$PATH
export PATH=$PATH:~/.bin
export PATH=/usr/local/share/python/:$PATH

# Prevent double file (._) file on OSX
export COPYFILE_DISABLE=true

# export ANDROID_SDK_ROOT=/usr/local/Cellar/android-sdk/r6

# rbenv
if [[ -s "/usr/local/bin/rbenv" ]]
then
  export PATH="$HOME/.rbenv/shims:$PATH"
  eval "$(rbenv init -)"
fi

# rvm
if [[ -s "$HOME/.rvm/scripts/rvm" ]]
  then source "$HOME/.rvm/scripts/rvm"
fi

alias svn=colorsvn

# Loading local env settings
if [ -f ~/.env.local ]
then
  . ~/.env.local
fi
