#!/usr/bin/env ruby
require 'webrick'
include WEBrick

s = HTTPServer.new(
  :BindAddress => 'localhost',
  :Port => 8000,
  :DocumentRoot => Dir::pwd,
  :Logger => nil
)
trap("INT"){ s.shutdown }
s.start
